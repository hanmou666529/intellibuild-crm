1. 文档概述
1.1 目的
本文档旨在规范《智营CRM》系统中“客户-合同-订单-回款”核心业务链路的数据流转逻辑，解决历史遗留的数据孤岛、关联断裂及金额口径不一致问题，实现真正的“业财一体化”闭环。
1.2 适用范围
本规范适用于CRM系统中客户管理、合同管理、订单管理、财务回款四大核心模块的数据库设计、接口校验及前端交互逻辑。
2. 核心数据模型与关联规范（解决“找不到客户”问题）
2.1 强关联原则
系统中所有业务单据必须通过  进行强关联，严禁仅通过文本名称进行模糊匹配。
2.2 数据库约束
•合同表 (crm_contract)： 字段必须设置为 ，并建立外键约束指向 。
•订单表 (crm_order)： 字段必须设置为 ，并建立外键约束指向 。
•回款表 (crm_payment)： 字段必须设置为 ，确保每笔回款都能追溯至具体合同。
2.3 前端交互规范
•入口限制：合同与订单的创建入口必须强制从【客户详情页】发起，系统自动带入 ，隐藏手动选择客户的下拉框，从根源上杜绝“孤儿数据”。
•临时创建机制：若当前客户不在系统中，允许在合同创建弹窗内点击“新建客户”，系统后台采用事务机制，先创建客户记录获取ID，再创建合同记录，保证数据完整性。
3. 核心金额字段定义与计算逻辑（解决“金额以谁为准”问题）
3.1 数据库字段设计
在  表中，必须独立定义以下金额字段，禁止单一金额字段混用：
字段名	中文名称	数据类型	计算逻辑与说明
	合同总额	DECIMAL(18,2)	合同约定的总金额上限。可通过“补充协议”变更。
	已确认成交额	DECIMAL(18,2)	业务口径。等于该合同下所有状态为“已完成”的订单金额之和。
	已回款金额	DECIMAL(18,2)	财务口径。等于该合同下所有状态为“已核销”的回款记录金额之和。
	未回款金额(应收)	DECIMAL(18,2)	动态计算：。
3.2 金额更新触发器
•订单状态变更：当订单状态流转为“已完成/已签收”时，触发  累加。
•回款核销：当回款记录完成核销时，触发  累加，并自动重算 。
•退货处理：退货单生成“红字订单”，状态为“已完成”时，触发  扣减。
4. 业务执行流与状态机流转规范
4.1 标准执行顺序
1.客户建档：完善必填项，状态流转为“跟进中”。
2.合同创建：从客户详情页发起，提交审批，状态变为“已生效”。
3.订单执行：基于已生效合同创建，校验可用额度，发货后状态变为“已完成”。
4.回款核销：财务录入流水，关联合同核销，更新回款进度。
4.2 核心校验规则（拦截器）
•合同额度校验：创建订单时，校验 。超额则拦截或触发特批流。
•信用风控校验：创建订单时，校验该客户的  是否超过其信用额度。若超期欠款过多，系统自动锁单。
5. 报表统计口径规范
为满足不同部门的管理诉求，系统报表需采用以下取值逻辑：
报表类型	核心指标	取值字段	业务解释
销售业绩报表	个人/团队成交额		货已交付，业绩已达成。
财务资金报表	实际现金流入		钱已到账，可用于资金调度。
经营风控报表	应收账款余额		货已发但未收款，需重点催收。
合同执行报表	合同剩余可用额度		评估客户后续采购潜力。
6. 历史数据清洗方案（实施第一步）
在上线新逻辑前，需执行以下数据修复脚本：
1.孤儿数据修复：通过NLP提取无  合同中的甲方名称，模糊匹配客户库，人工确认后批量绑定。
2.金额重算：对所有历史合同，根据现有的订单表和回款表，重新跑批计算并回填  和  字段。
文档版本： V1.0
编写日期： 2026-07-02
适用项目： 智营CRM（AI驱动建材行业版）
(AI生成)